#include <stdio.h>
/**
 *main - Entry point into a code that prints puts function
 *Return: 0(Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
